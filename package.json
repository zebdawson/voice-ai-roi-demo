{
  "name": "voice-ai-roi-demo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"node src/server/index.js\" \"cd src/client && npm run dev\"",
    "build": "cd src/client && npm run build",
    "start": "node src/server/index.js",
    "install:all": "npm install && cd src/client && npm install"
  },
  "dependencies": {
    "body-parser": "^1.20.2",
    "concurrently": "^8.2.0",
    "cors": "^2.8.5",
    "express": "^4.18.2"
  }
}
